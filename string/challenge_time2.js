// 😀12: challenge Time 🏁

// Return the Unicode of the last character in a string
const myName="Abhay"
const lastIndex=myName.length-1
const uniCode=myName.charCodeAt(lastIndex)
console.log(uniCode);